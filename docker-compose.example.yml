services:
  # ===== Broker MQTT (l'ESP people counter pubblica qui) =====
  mosquitto:
    image: eclipse-mosquitto:2
    container_name: person-counter-mqtt
    restart: unless-stopped
    environment:
      MQTT_USER: "espuser"
      MQTT_PASS: "esp-cambia-questa-password"
    # crea il file password dalle credenziali, poi avvia il broker
    command:
      - sh
      - -c
      - |
        rm -f /mosquitto/config/pwfile
        mosquitto_passwd -c -b /mosquitto/config/pwfile "$$MQTT_USER" "$$MQTT_PASS"
        chown mosquitto:mosquitto /mosquitto/config/pwfile
        chmod 600 /mosquitto/config/pwfile
        exec mosquitto -c /mosquitto/config/mosquitto.conf
    volumes:
      - ./mosquitto/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
    ports:
      - "1883:1883"        # esponi il broker all'ESP sulla rete locale

  # ===== App: subscriber MQTT + dashboard =====
  counter:
    build: .
    image: person-counter:latest
    container_name: person-counter
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      TZ: "Europe/Rome"
      CAMERA_NAME: "ingresso"

      # ---- MQTT input (STESSE credenziali del broker) ----
      MQTT_HOST: "mosquitto"
      MQTT_PORT: "1883"
      MQTT_USER: "espuser"
      MQTT_PASS: "esp-cambia-questa-password"
      MQTT_TOPIC: "people_counter/#"          # adatta al topic del tuo ESP
      MQTT_ENTER_TOKENS: "enter,in,entrata"   # parole che indicano un ingresso
      MQTT_EXIT_TOKENS: "exit,out,uscita"     # parole che indicano un'uscita

      # ---- Auth dashboard (vuote = pubblica) ----
      AUTH_USER: "admin"
      AUTH_PASS: "cambia-questa-password"
      SECRET_KEY: "metti-qui-una-stringa-random-lunga"
      SESSION_DAYS: "7"

      # ---- PostgreSQL (vuote = solo JSON) ----
      DB_HOST: "postgres"
      DB_PORT: "5432"
      DB_NAME: "person_counter"
      DB_USER: "counter"
      DB_PASS: "counter-pass"
      DB_SCHEMA: "public"
      DB_TABLE: "counter_events"
      DB_SSLMODE: "prefer"

      # ---- Webhook opzionale ----
      WEBHOOK_ENABLED: "0"
      WEBHOOK_URL: ""
      WEBHOOK_HEADERS: ""
      WEBHOOK_TIMEOUT: "4"
      WEBHOOK_RETRY: "1"
    depends_on:
      - mosquitto
      - postgres

  postgres:
    image: postgres:16-alpine
    container_name: person-counter-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: person_counter
      POSTGRES_USER: counter
      POSTGRES_PASSWORD: counter-pass
    volumes:
      - ./pgdata:/var/lib/postgresql/data
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
    ports:
      - "5432:5432"
